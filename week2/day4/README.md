# REGEX BASIC ASSIGNMENT

This file contains basic REGEX concepts and practice queries using SQL `REGEXP_SUBSTR()` function with regex_practice table.


## Topics Covered

- Digits extraction
- Alphabets extraction
- Email regex
- Phone regex
- Start `^` and End `$`
- Exact match `{ }`
- Continuous match `+`

---
